services:
  copyparty:
    image: copyparty/ac:1.20.7
    container_name: copyparty
    restart: unless-stopped
    volumes:
      - ./config/copyparty.conf:/cfg/copyparty.conf:ro
      - ./data/db:/cfg/hists
      - ./data/library:/w
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:3923/', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 15s
    networks:
      - caddy-net

networks:
  caddy-net:
    external: true
